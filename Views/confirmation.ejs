<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Order Confirmation</title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">

  <h1>Thank You for Your Purchase!</h1>
  <p>Your order has been received and is being processed.</p>
  
  <!-- Order number from server -->
  <p>Order Number: <strong><%= orderNumber %></strong></p>

  <!-- Customer email from session -->
  <p>We've sent a confirmation email to <strong><%= customerEmail %></strong>.</p>

  <!-- Optional: show total -->
  <% if (typeof total !== 'undefined') { %>
    <p>Total Paid: €<strong><%= total %></strong></p>
  <% } %>

  <a href="/product" class="btn btn-primary mt-4">Continue Shopping</a>

</body>
</html>

